# ===================================================================
# RepoWise Backend - Python Dependencies
# Phase 3: Knowledge Graph Integration with Neo4j
# ===================================================================

# ============== Phase 1: Core Dependencies (ACTIVE) ==============

# FastAPI and Server Components
fastapi==0.121.0
uvicorn[standard]==0.38.0
python-multipart==0.0.20
pydantic==2.12.4
pydantic-settings>=2.8.0

# Environment Variables and Configuration
python-dotenv==1.2.1

# GitHub API Integration
PyGithub==2.4.0
GitPython==3.1.43
requests==2.32.3

# GitLab API Integration
python-gitlab==4.4.0

# LLM and Language Models
groq>=0.4.2
langchain==0.1.0
# langchain-core otomatik inecek, elle yazma
# langchain-community otomatik inecek, elle yazma
langchain-openai==0.0.5
tiktoken==0.5.2

# HTTP Client
aiohttp>=3.9.0

# ============== Phase 2: RAG Pipeline (ACTIVE) ==============

# Vector Database
chromadb==0.4.22
# Note: ChromaDB includes its own SQLite backend

# Code Embeddings - Multiple Options:

# Option 1: Sentence Transformers (Recommended for CodeBERT)
sentence-transformers==2.3.1
# Includes: transformers, torch, tokenizers

# Option 2: HuggingFace Transformers (Direct)
transformers==4.37.2

# PyTorch (Required for embeddings)
torch==2.2.0
# Note: CPU version, for GPU use torch with CUDA

# Text Processing
nltk==3.8.1

# Code Parsing
tree-sitter==0.21.0
tree-sitter-python==0.21.0
tree-sitter-javascript==0.21.0

# Utilities
numpy>=1.24.0
pandas>=2.0.0

# ============== Phase 3: Knowledge Graph (ACTIVE!) ==============

# Neo4j Database Driver
neo4j==5.14.0

# Code Analysis and AST Parsing
tree-sitter==0.20.4
tree-sitter-python==0.20.4
tree-sitter-javascript==0.20.3
tree-sitter-java==0.20.2
tree-sitter-cpp==0.20.0
tree-sitter-go==0.20.0

# Additional Language Parsers (Optional - Uncomment if needed)
# tree-sitter-typescript==0.20.3
# tree-sitter-rust==0.20.4
# tree-sitter-c-sharp==0.20.0
# tree-sitter-ruby==0.20.0
# tree-sitter-php==0.20.0

# Graph Database Utilities (Optional - using neo4j driver primarily)
# py2neo==2021.2.3

# ============== Development and Testing (Optional) ==============
# pytest>=7.4.0
# pytest-asyncio>=0.21.0
# httpx>=0.24.0
# black>=23.0.0
# flake8>=6.0.0

# ===================================================================
# Installation Commands:
#
# Full install (All Phases):
#   pip install -r requirements.txt --break-system-packages
#
# Phase 1 only (Core API):
#   pip install fastapi uvicorn PyGithub python-dotenv --break-system-packages
#
# Phase 2 only (RAG Pipeline):
#   pip install chromadb==0.4.22 sentence-transformers==2.3.1 --break-system-packages
#
# Phase 3 only (Knowledge Graph):
#   pip install neo4j==5.14.0 tree-sitter tree-sitter-python --break-system-packages
#
# ChromaDB only:
#   pip install chromadb==0.4.22 --break-system-packages
#
# Neo4j only:
#   pip install neo4j==5.14.0 --break-system-packages
#
# CodeBERT embeddings:
#   pip install sentence-transformers==2.3.1 --break-system-packages
#
# PyTorch CPU (faster install):
#   pip install torch==2.2.0 --index-url https://download.pytorch.org/whl/cpu
#
# Check installations:
#   python -c "import chromadb; print(f'ChromaDB: {chromadb.__version__}')"
#   python -c "from sentence_transformers import SentenceTransformer; print('Sentence Transformers: OK')"
#   python -c "from neo4j import GraphDatabase; print('Neo4j Driver: OK')"
#   python -c "import tree_sitter; print('Tree-sitter: OK')"
# ===================================================================

# ===================================================================
# Phase Progress:
#
# ✅ Phase 1: Core API & GitHub Integration (COMPLETE)
#    - FastAPI backend
#    - GitHub/GitLab service
#    - Repository analysis
#    - 15 API endpoints
#
# ✅ Phase 2: RAG Pipeline (COMPLETE)
#    - ChromaDB vector storage
#    - Sentence-transformers embeddings
#    - Document processing
#    - Semantic search
#    - 7 new RAG endpoints
#
# 🚀 Phase 3: Knowledge Graph (IN PROGRESS)
#    - Neo4j graph database
#    - Code entity extraction (tree-sitter)
#    - Relationship mapping
#    - Graph-based queries
#
# 📅 Phase 4: Multi-Agent System (PLANNED)
# 📅 Phase 5: Evaluation & Publishing (PLANNED)
# ===================================================================